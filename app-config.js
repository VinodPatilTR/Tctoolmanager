// Optional shared API configuration for static hosting.
// Set this to your deployed backend URL so all users read/write the same ToolConfig.json.
// Example:
// window.TCTOOL_API_BASE = 'https://your-backend-domain.com/api/tools';

window.TCTOOL_API_BASE = window.TCTOOL_API_BASE || '';
